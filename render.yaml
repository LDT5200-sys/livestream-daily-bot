services:
  - type: web
    name: livestream-daily-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 5000
