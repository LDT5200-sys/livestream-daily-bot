services:
  - type: web
    name: livestream-daily-bot
    env: python
    buildCommand: pip install -r requirements.txt && cp settings.ini.example settings.ini
    startCommand: gunicorn web:app --bind 0.0.0.0:$PORT
